# Versions figées (étape 45) : résolues ensemble via `pip install --dry-run` (pas juste la dernière version
# de chaque paquet prise indépendamment — certaines dernières versions indépendantes, ex: librosa 1.0.0 ou
# scipy 1.18.1, se sont révélées incompatibles entre elles une fois résolues toutes ensemble). Sans ça, deux
# installations de la même version de Jaris, à quelques semaines d'écart, pouvaient récupérer des versions
# différentes de chaque dépendance — jamais testées ensemble, un risque inutile pour un simple sidecar voix.
#
# Étape 158 : la transcription passe de Cohere Transcribe à Parakeet v3 (onnx-asr, en RAM). torch, transformers,
# accelerate, sentencepiece et librosa ne servaient QU'À Cohere : retirés (plus de 3 Go de moins à installer, dont
# torch ~2,5 Go). Ensemble restant re-résolu depuis un environnement vide (`pip install --dry-run --report`).
huggingface_hub==1.30.0
# Lu par supertonic (synthèse vocale), pas directement par Jaris.
soundfile==0.14.0
scipy==1.17.1
# Demandé par onnxruntime (pas par Jaris directement) : figé à la version déjà résolue et éprouvée.
protobuf==7.36.1
sounddevice==0.5.6
numpy==2.4.6
supertonic==1.3.1
# Déjà installé de fait comme dépendance transitive de supertonic (vérifié : la même version, 1.30.0, est
# celle que pip résout) : épinglé ici explicitement car wakeword.py l'importe maintenant directement pour le
# mot d'activation "Jaris" (voir python/models/, jaris.onnx) — sans cette ligne, une évolution future de
# supertonic qui abandonnerait onnxruntime casserait wakeword.py silencieusement, sans rapport avec la TTS.
onnxruntime==1.30.0
# Transcription (étape 158) : charge Parakeet TDT 0.6B v3 (NVIDIA) avec onnxruntime, sur le processeur.
onnx-asr==0.12.0
