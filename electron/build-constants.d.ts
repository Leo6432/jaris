/**
 * Valeurs figées au moment de la construction par Vite (`define` dans electron.vite.config.ts), pas des
 * variables lues à l'exécution : elles sont littéralement remplacées par leur contenu dans le code
 * compilé. Déclarées ici pour que TypeScript les connaisse.
 */
declare const __GOOGLE_CLIENT_ID__: string
declare const __GOOGLE_CLIENT_SECRET__: string
